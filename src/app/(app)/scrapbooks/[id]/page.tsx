'use client';
// This page is no longer in use, but is kept to satisfy the build process
// for static export, as dynamic route files cannot be easily removed.
// The new editor is at /app/(app)/editor/page.tsx.

export default function DeprecatedScrapbookPage() {
  // In a real app, you might want to redirect to the new editor page.
  // For now, we return null to keep things simple and avoid build errors.
  return null;
}
